# Rock Paper Scissors!
This repo contains a set of services that implement rock-paper-scissors as a Discord bot.

You can start it up by running `docker compose up`.

Once running, you can access Grafana to visualize perfomance metrics at http://localhost:3000/ (user name: `admin` password: `admin`).
